openapi: 3.0.0
info:
  title: API
  version: v1
paths:
  /dynamic:
    get:
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Resp'
components:
  schemas:
    Resp:
      type: object
      properties:
        anyProp: {}
        anyList:
          type: array
          items: {}
